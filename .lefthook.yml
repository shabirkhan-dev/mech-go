pre-commit:
  parallel: true
  commands:
    lint:
      glob: "./src/{js,ts,jsx,tsx}"
      run: pnpm lint:fix
    typecheck:
      glob: "./src/{js,ts,jsx,tsx}"
      run: pnpm tsc --noEmit

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}